const driver = 'bob';

//invokes console.log() with the string "this code was called"
console.log("this code was called")

//invokes console.log() with the `driver` variable
console.log(driver)

//invokes the console.log with the `driver` variable as the first argument and 
//`is the driver variable name` as the second argument
console.log(driver, "is the driver variable name")